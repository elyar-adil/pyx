[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyx"
version = "0.1.0"
description = "PyX static subset prototype"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
